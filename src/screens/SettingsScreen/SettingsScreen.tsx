import {Text, View} from 'react-native';
import {SettingsScreenProps} from '../../types';

export const SettingsScreen = ({}: SettingsScreenProps): JSX.Element => {
  return (
    <View>
      <Text>Settings Screen</Text>
    </View>
  );
};
